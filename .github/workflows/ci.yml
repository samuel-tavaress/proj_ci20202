name: CI

on:
  pull_request:
    branches: [master]

jobs:
  build: 
    runs-on: ubuntu-20.04
    steps:
      - name: show project files 
        run: ls -la
      - name: Checkout project 
        uses: actions/checkout@v2
      - name: show project files 
        run: ls -la

    